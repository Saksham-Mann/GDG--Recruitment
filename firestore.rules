rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Disallow direct client SDK reads and writes; server-side firebase-admin SDK retains full access
      allow read, write: if false;
    }
  }
}
